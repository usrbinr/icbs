% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icbs.R
\name{geom_marquee_colored}
\alias{geom_marquee_colored}
\title{Create a Color-Matched Label Layer}
\usage{
geom_marquee_colored(data, mapping, ..., bold = FALSE, italic = FALSE)
}
\arguments{
\item{data}{Data frame containing the data.}

\item{mapping}{Aesthetic mapping created by \code{aes()}. Must include \code{x}, \code{y},
\code{label}, and \code{color} (the color to use for the text).}

\item{...}{Additional arguments passed to \code{marquee::geom_marquee()}.}

\item{bold}{Make text bold. Default: FALSE.}

\item{italic}{Make text italic. Default: FALSE.}
}
\value{
A ggplot2 layer object.
}
\description{
A convenience function that adds colored text labels using marquee.
Pre-formats labels with marquee color syntax before plotting.
}
\examples{
\dontrun{
library(marquee)
df <- data.frame(
    x = c("A", "B", "C"),
    y = c(10, -5, 8),
    text_color = c("#006400", "#8B0000", "#006400"),
    label = c("+10", "-5", "+8")
)
ggplot(df, aes(x = x, y = y)) +
    geom_col(aes(fill = text_color)) +
    geom_marquee_colored(
        data = df,
        mapping = aes(x = x, y = y, label = label, color = text_color),
        vjust = -0.5
    )
}

}
