% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{inline_legend}
\alias{inline_legend}
\title{Create an Inline Legend Annotation}
\usage{
inline_legend(
  colors,
  x = 0,
  y = 0,
  orientation = "horizontal",
  sep = " • ",
  bold = TRUE
)
}
\arguments{
\item{colors}{Named vector of colors (names = category labels).}

\item{x}{X position for the legend. Default: 0.}

\item{y}{Y position for the legend. Default: 0.}

\item{orientation}{Either "horizontal" or "vertical". Default: "horizontal".}

\item{sep}{Separator between items for horizontal layout. Default: bullet.}

\item{bold}{Make text bold. Default: TRUE.}
}
\value{
A data frame with columns \code{x}, \code{y}, and \code{label} (marquee-formatted).
}
\description{
Creates a data frame for adding colored text annotations as an
inline legend replacement.
}
\details{
Returns a data frame with marquee-formatted labels that can be added
to a plot using \code{marquee::geom_marquee()}. This allows placing a
color-coded legend directly within the plot area rather than using
the standard ggplot2 legend. Consider using \code{legend_block()} instead
for patchwork compositions.
}
\examples{
\dontrun{
fill_colors <- c("Apples" = "#E63946", "Oranges" = "#F4A261")

# Add as annotation layer
ggplot(...) +
    geom_marquee(
        data = inline_legend(fill_colors, x = 1, y = 100),
        aes(x = x, y = y, label = label),
        hjust = 0
    )
}

}
