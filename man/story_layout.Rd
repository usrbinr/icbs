% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icbs.R
\name{story_layout}
\alias{story_layout}
\title{Compose a Story Layout}
\usage{
story_layout(
  plot,
  title = NULL,
  subtitle = NULL,
  narrative = NULL,
  caption = NULL,
  narrative_position = "right",
  narrative_width = 0.35,
  title_size = 16,
  subtitle_size = 11,
  narrative_size = 10,
  caption_size = 9,
  title_height = NULL,
  subtitle_height = NULL,
  caption_height = NULL,
  auto_heights = TRUE,
  output_width = 12
)
}
\arguments{
\item{plot}{The main ggplot visualization.}

\item{title}{Optional title - either text (marquee-formatted) or a ggplot object
created by \code{\link[=title_block]{title_block()}}. When text is provided, it's converted to a
title_block with the specified title_size.}

\item{subtitle}{Optional subtitle - either text (marquee-formatted) or a ggplot
object created by \code{\link[=subtitle_block]{subtitle_block()}}.}

\item{narrative}{Optional narrative - either text (marquee-formatted) or a ggplot
object created by \code{\link[=text_narrative]{text_narrative()}}.}

\item{caption}{Optional caption - either text or a ggplot object created by
\code{\link[=caption_block]{caption_block()}}.}

\item{narrative_position}{Position of narrative: "right", "left", "bottom".
Default: "right".}

\item{narrative_width}{Width proportion for narrative (0-1). Default: 0.35.}

\item{title_size}{Size for title in pts. Default: 16 (large, prominent).}

\item{subtitle_size}{Size for subtitle in pts. Default: 11.}

\item{narrative_size}{Size for narrative text in pts. Default: 10.}

\item{caption_size}{Size for caption in pts. Default: 9.}

\item{title_height}{Height proportion for title row (0-1). When NULL and
\code{auto_heights = TRUE}, this is calculated automatically based on text length.}

\item{subtitle_height}{Height proportion for subtitle row (0-1). When NULL and
\code{auto_heights = TRUE}, this is calculated automatically.}

\item{caption_height}{Height proportion for caption row (0-1). When NULL and
\code{auto_heights = TRUE}, this is calculated automatically.}

\item{auto_heights}{Logical. If TRUE (default), automatically calculates
appropriate heights for title/subtitle/caption based on text length to
prevent clipping. Set to FALSE to use fixed heights.}

\item{output_width}{Numeric. Expected output width in inches (default: 12).
Used by auto_heights to estimate how many lines text will wrap to.}
}
\value{
A patchwork object.
}
\description{
Combines a chart with optional title, narrative text, and caption
into a complete story layout using patchwork. Each component is a
modular plot element, giving full control over sizing and positioning.
}
\details{
This function creates layouts inspired by
\href{https://www.storytellingwithdata.com/}{Storytelling with Data} principles,
with large, prominent titles and supporting narrative text.
}
\examples{
\dontrun{
library(ggplot2)
library(marquee)

p <- ggplot(mtcars, aes(x = factor(cyl), y = mpg)) +
    geom_col(fill = "#404040") +
    theme_ibcs()

# Basic usage with text
story_layout(
    plot = p,
    title = "**Fuel Efficiency by Cylinder Count**",
    subtitle = "{#E63946 4-cylinder} cars lead in MPG",
    narrative = "The data shows a clear trend:\n\n**Fewer cylinders = better mileage.**",
    caption = "Source: Motor Trend, 1974"
)

# Full control with block functions
story_layout(
    plot = p,
    title = title_block(
        "**Fuel Efficiency by Cylinder Count**",
        title_size = 20
    ),
    subtitle = subtitle_block(
        "{#E63946 4-cylinder} cars lead in MPG",
        subtitle_size = 12
    ),
    narrative = text_narrative(
        "The data shows a clear trend:\n\n**Fewer cylinders = better mileage.**",
        size = 11
    )
)
}

}
